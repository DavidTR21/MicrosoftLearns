// Dadas las siguientes variables, devuelve el valor más alto de entre ambas

int firstValue = 500;
int secondValue = 600;

// Declaramos variable largerValue y usamos el método Math para extraer el mayor de los dos valores
int largerValue = System.Math.Max(firstValue, secondValue);
Console.WriteLine(largerValue);